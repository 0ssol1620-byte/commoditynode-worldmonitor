# CommodityNode Changes

This file records material changes from the frozen World Monitor baseline
`eb51542990b244fdde1314d6682f813c9e29ff70`.

## 2026-07-27 — Foundation

- created the official public GitHub fork and recorded its upstream baseline;
- preserved the upstream `LICENSE` and copyright notices;
- added fork, source-offer, trademark, third-party, and data-source notices;
- added a centralized variant registry and the additive `commoditynode`
  variant;
- defined a minimal CommodityNode panel set and separate desktop/mobile layer
  budgets;
- added CommodityNode metadata, brand tokens, public source page, and build
  provenance generation;
- added CI-facing license/source and variant contract checks;
- created a patch ledger for every direct change to upstream core files.

The detailed file-by-file record is in `docs/fork/patch-ledger.yml`.
